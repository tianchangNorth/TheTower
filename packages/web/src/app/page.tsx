import { CommandWorkbench } from "@/components/workbench/CommandWorkbench";

// Phase 1a：Command 首页，默认进入新建 thread 上下文（threadId 缺省）。
export default function HomePage() {
  return <CommandWorkbench />;
}
