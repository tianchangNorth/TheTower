import { PagePlaceholder } from "@/components/shell/PagePlaceholder";

export default function WorkspacesPage() {
  return (
    <PagePlaceholder
      title="Workspaces"
      description="工作区与文件活动。Phase 5 落地 trusted workspaces、thread 绑定、最近工具访问；文件树/diff/git/terminal 后置。"
    />
  );
}
