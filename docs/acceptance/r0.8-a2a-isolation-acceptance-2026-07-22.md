# R0.8 A2A isolation 验收记录（2026-07-22）

## 结论

**正式通过，R0.8 已关闭。**

- 契约、Mock/fixture、历史 migration fixture、实际数据库副本演练和 stream 存储预算通过；
- Codex、Claude 真实 Provider 验收均为 `terminalStatus=done`，各自 11 项检查全部通过；
- 两份 stream 均按 invocation/agent 合并为一行，未触发 payload 预算；
- 真实 Provider、历史 migration 和 stream 观察三个上线门禁均已满足。

## 自动化门禁

执行：

```bash
pnpm test:ci
```

结果：

- lint 与 production build 通过；
- API unit：118/118；
- API integration：3/3；
- migration：2/2；
- Playwright：6/6。

关键隔离回归：

- Runner `text` 不再生成隐式公开 callback；
- stdout/final 统一落入 `agent_stream`；
- stdout 后追加 thinking 不再清空已有 stdout；
- debug 模式下其他 Agent 可看到普通 stream，但 mixed stream 的 thinking 字段会被剥离；
- 250 个 stdout chunk + 50 个 thinking chunk 保持单行存储。

## 实际数据库 stream 观察

数据源：`packages/api/data/app.db`（只读）。

```bash
pnpm observe:streams -- \
  --db packages/api/data/app.db \
  --output test-results/r0.8-local-acceptance/stream-storage-report.json
```

结果：

| 指标 | 结果 | 预算 |
| --- | ---: | ---: |
| stream rows | 7 | — |
| invocation+agent groups | 7 | — |
| max rows/group | 1 | 1 |
| total payload | 21,289 bytes | — |
| P95 payload/group | 9,202 bytes | 1,048,576 bytes |
| max payload/group | 9,202 bytes | 1,048,576 bytes |
| breaches | 0 | 0 |

结论：当前实际数据没有行数或 payload 超限。最大分组仅使用默认预算的约 0.88%。

## 实际数据库 migration rehearsal

```bash
pnpm rehearse:migration -- \
  --db packages/api/data/app.db \
  --output test-results/r0.8-local-acceptance/migration
```

结果：

- `sourceUnchanged=true`；
- 源库已经完成 v1，因此 legacy `agent_final=0`、debug thread `=0`；
- migration versions 为 `[1]`；
- 二次执行状态一致，`idempotent=true`；
- 总体 `ok=true`。

历史转换本身由独立 fixture 继续覆盖：一条 `agent_final` 被迁移为 `callback`，原有 `extra` 保留并增加 `isExplicitPost=false`，thread 从 debug 迁移为 play。

## 真实 Codex/Claude 验收

执行命令：

```bash
pnpm test:e2e:real
```

本机检测到：

- Codex CLI `0.145.0`；
- Claude Code `2.1.217`。

2026-07-22 真实执行结果：

- Codex：报告生成于 `2026-07-22T08:48:59.941Z`，`terminalStatus=done`，11 项 checks 全部为 `true`；stream 为单行、891 bytes、无预算超限；
- Claude：报告生成于 `2026-07-22T08:49:07.737Z`，`terminalStatus=done`，11 项 checks 全部为 `true`；stream 为单行、1,047 bytes、无预算超限；
- 两个 Provider 的 private callback 均只进入 source/observer 上下文；play 隐藏 stream，debug 向 observer/outsider 共享普通 stream 且剥离 thinking。

报告位置：

- `test-results/real-a2a/codex-a2a-isolation.json`，SHA-256 `776f256b870b7cd388616b7269caa2929b01ed48ef3ab3a6dc50e0a819265ff7`；
- `test-results/real-a2a/claude-a2a-isolation.json`，SHA-256 `960e4c9695b02b13657d7cc1db27ce3c6fab0011a73aca5776d12a0b88a6732d`。

结论：两份真实 Provider 报告满足关闭条件，R0.8 正式完成。
